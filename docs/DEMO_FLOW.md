# Sigap Demo Flow — RETIRED

> **This document is retired.** The canonical, presentation-ready demo guide now
> lives in **[LOCAL_DEMO_RUNBOOK.md](./LOCAL_DEMO_RUNBOOK.md)** (English,
> authoritative) with its synchronized Bahasa Indonesia twin at
> **[LOCAL_DEMO_RUNBOOK.id.md](./LOCAL_DEMO_RUNBOOK.id.md)**.
>
> This file is kept only so existing links do not break. Its previous content
> described an older runtime (port 8080/[::1], manual multi-terminal startup,
> 6-step smoke) that no longer matches the current bare-metal bootstrap
> (`scripts/dev/Start-LocalDev.ps1`, API on port 18080, 8-step smoke).
>
> Do not add new content here. Update the runbook instead — both language
> versions must be updated in the same PR when the demo flow changes.
